const colors = {
  primary: '#000',
  secondary: '#b82a28',
  white: '#fff',
  shadow: '#777',
};

const spacing = {
  base: '1rem',
  small: '0.5rem',
};

const boxShadow = {
  base: `1px 1px 5px 0px ${colors.shadow}`,
}

export {
  colors,
  spacing,
  boxShadow,
};